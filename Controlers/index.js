const registerAuthControler = require('./registerAuthControler')
const loginControler = require('./loginContoler')
const createAuctionControler = require('./createAuctionControler')
const bidderControler = require('./bidderControler')

module.exports = {
    registerAuthControler,
    loginControler,
    createAuctionControler,
    bidderControler
}